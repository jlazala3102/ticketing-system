{
  "name": "ticketing-system",
  "version": "1.0.0",
  "description": "A modern, frontend-focused ticketing system built with React, Node.js, Express, MongoDB, and Tailwind CSS",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "ticketing",
    "react",
    "nodejs",
    "express",
    "mongodb",
    "tailwind",
    "frontend"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
